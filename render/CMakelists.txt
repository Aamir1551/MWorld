cmake_minimum_required(VERSION 3.10)
set(CMAKE_CXX_STANDARD 11)
set(glfw "${CMAKE_CURRENT_SOURCE_DIR}/lib/glfw-3.3.2/build/src/libglfw3.a")

project(render)
add_subdirectory("${CMAKE_CURRENT_SOURCE_DIR}/lib/glad")
add_subdirectory("${CMAKE_CURRENT_SOURCE_DIR}/cube")
add_subdirectory("${CMAKE_CURRENT_SOURCE_DIR}/../utils" "./util_build")

add_executable(tut main.cpp)

target_include_directories(tut PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/lib/glfw-3.3.2/include/")
target_include_directories(tut PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/cube")
target_include_directories(tut PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/lib/glm")
#[[ glad is compiling even though no target_include_directory has been given, why is this??? ]]

target_link_libraries(tut utils linear_alg cube glad "${glfw}")