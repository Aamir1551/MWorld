#ifndef UTILS_SETTINGS_SETTINGS_H
#define UTILS_SETTINGS_SETTINGS_H

// TODO
// 1) fix bug that causes prototype_two to not work when real = double
namespace settings
{
    //have an enum of types that real can be. {float, double long long double, long double, long float etc}
    using real = float;
} // namespace settings
#endif